#include <stdio.h>

// But we are able to do this .............

// but this is stack array 
  int main (){

    int size ; 
    printf ( "Enter the size of the array :: \n");
    scanf ( "%d", size);

    int array[size];

    
    return 0;
  }